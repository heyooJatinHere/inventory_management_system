services:

  backend:
    build:
      context: ./inventory_backend

    ports:
      - "8000:8000"

    restart: always

  frontend:
    build:
      context: ./inventory_frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend

    restart: always